version: 2.1
jobs:
  test:   
    machine:
      image: default
    resource_class: medium
    steps:
      - checkout
      - run: echo "Hello executor!"
      
workflows:
  example:
    jobs:
      - test
